
  TestGen.asm:

    It reads a digit number input and then calls gen.asm to produce a cos(n0) wave output; 
   
  TestDet.asm:

    It takes the cos(n0) as input and then calls the det.asm and tst.asm to give the initial input digit number.

  File:    
    gen_in.io       ;input telephone number
    gen_out.io      ;gen.asm output wave number
    det_out.io      ;det.asm output detector telephone number



